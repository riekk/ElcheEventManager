@model ElcheEventManager.Models.db.Status

@{
    ViewBag.Title = "Detalles estados";
}

<h2>Detalles</h2>

<div>
    <h4>Estados</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id }, new { @class = "btn btn-outline-dark" }) 
    @Html.ActionLink("Lista de estados", "Index", null, new { @class = "btn btn-outline-dark" })
</p>
